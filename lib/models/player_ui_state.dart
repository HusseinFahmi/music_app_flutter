class PlayerUiState {
  final bool isPlaying;
  final bool isRepeatEnabled;
  final bool isRandomEnabled;

  const PlayerUiState(
      {required this.isPlaying, required this.isRepeatEnabled, required this.isRandomEnabled});
}
